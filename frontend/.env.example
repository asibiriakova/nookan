# Base URL of the FastAPI backend (see backend/, run with `uv run uvicorn nookan_backend.main:app --reload`).
# Copy this file to .env.local and adjust if the backend isn't on the default port.
VITE_API_BASE_URL=http://localhost:8000
